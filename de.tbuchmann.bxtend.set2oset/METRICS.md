# Code Metrics — de.tbuchmann.bxtend.set2oset

_Generated by `code_metrics.py` (repo root). Counts only hand-written `.xtend` source; see Methodology below for what is excluded and why._

## Summary (pure code — comments excluded)

| Metric | Value |
|---|---|
| Lines of code (LOC, non-blank/non-comment) | 135 |
| Blank lines | 33 |
| Total lines counted (code + blank + comment) | 443 |
| Words (code only) | 450 |
| Characters, code only (incl. whitespace) | 4878 |
| Characters, code only (non-whitespace) | 4199 |
| Hand-written methods | 9 |
| Avg. characters / word | 9.33 |
| Avg. LOC / method | 15.0 |

## Comments (excluded from the code metrics above, reported separately)

| Metric | Value |
|---|---|
| Comment lines | 275 |
| Comment words | 2095 |
| Comment characters (incl. whitespace) | 15155 |
| Comment characters (non-whitespace) | 12435 |

Files: 2 fully hand-written, 1 mixed (generated skeleton stripped out), 1 fully generated/excluded — 4 `.xtend` files total.

## Per-file breakdown

Code columns (LOC/Words/Chars) exclude comments; the two Comment columns report comment-only word/character counts separately.

| File | Status | LOC | Blank | Words | Chars | Methods | Comment Lines | Comment Words | Comment Chars |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `src/de/tbuchmann/bxtend/set2oset/rules/Elem2Elem.xtend` | generated (excluded) | — | — | — | — | — | — | — | — |
| `src/de/tbuchmann/bxtend/set2oset/rules/Element2Element.xtend` | handwritten | 67 | 8 | 241 | 2580 | 4 | 123 | 996 | 7079 |
| `src/de/tbuchmann/bxtend/set2oset/rules/MySet2MyOrderedSet.xtend` | handwritten | 56 | 8 | 181 | 2026 | 4 | 69 | 529 | 3697 |
| `src/de/tbuchmann/bxtend/set2oset/rules/Set2osetTransformation.xtend` | mixed (skeleton stripped) | 12 | 17 | 28 | 272 | 1 | 83 | 570 | 4379 |

## Excluded generated content (detail)

- `src/de/tbuchmann/bxtend/set2oset/rules/Elem2Elem.xtend` — fully excluded, 247 raw lines (base rule class, identical shape across all examples).
- `src/de/tbuchmann/bxtend/set2oset/rules/Set2osetTransformation.xtend` — 204 raw lines of generated skeleton excluded (methods: new, new, addRules, sourceToTarget, targetToSource, checkCorrespondences, detectSourceDeletions, detectTargetDeletions, deleteUnreferencedTargetElements, deleteUnreferencedSourceElements); hand-written methods kept: synch.

## Methodology

- Only `.xtend` files under `src/` are considered; all other file types are ignored.
- `package` and `import` lines are stripped from every counted file before any metric is computed.
- `Elem2Elem.xtend` is excluded entirely — identical-shape abstract base rule class in every example, would be generator output.
- `<Example>Transformation.xtend` is counted, minus its generated-skeleton methods: `addRules`, `addRulesBwd`, `addRulesFwd`, `checkCorrespondences`, `deleteUnreferencedSourceElements`, `deleteUnreferencedTargetElements`, `detectSourceDeletions`, `detectTargetDeletions`, `new`, `sourceToTarget`, `targetToSource`. Everything else in that file (notably `synch()`, and any project-specific helper) is hand-written and counted.
- LOC = non-blank, non-comment lines. Comment lines include `//` lines and `/* ... */`/Javadoc blocks (tracked line-by-line).
- Words/Characters in the code metrics are computed from LOC lines only — comment text is excluded from those totals and reported separately under Comments.
- Words = whitespace-split tokens. Characters counted both including and excluding internal whitespace.
- Methods = `def`/`override`/constructor (`new(...)`) signatures found in the counted scope.
- This is a heuristic brace-depth/regex-based counter tuned to this codebase's formatting conventions, not a certified SLOC tool.
